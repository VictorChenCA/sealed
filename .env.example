# Provided by EigenCompute (do not set manually in production):
# MNEMONIC=...           # auto-injected by KMS, sealed to enclave

# App config
APP_PORT=3000
DEFAULT_CIRCLE_TARGET_N=5

# Model config (paths inside the container)
MODEL_PATH=/app/models/qwen2.5-3b-instruct-q4_k_m.gguf
MODEL_SHA256_PUBLIC=baa1f9a44f3a9fc7e7d8c2a6e8f1e3a5d7b8c9e0f1a2b3c4d5e6f7a8b9c0d1e2

# Build info (baked at image build time, exposed via /verify)
COMMIT_SHA_PUBLIC=dev
IMAGE_DIGEST_PUBLIC=dev

# Rubric version
RUBRIC_VERSION_PUBLIC=v1.0
